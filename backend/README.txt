-- Đưa file data vào thư mục data để sử dụng dữ liệu cá nhân.
-- Nếu đưa dữ liệu mới vào trong thư mục data, cần chạy lại dòng sau trong terminal: 
$ python populate_database --reset

-- Để chạy backend api, vào terminal chạy lệnh sau:
$ uvicorn main:app --reload

